agent:
  system_template: |
    You are HypothesisAgent, a software security analyst responsible ONLY for producing a ranked list of vulnerability hypotheses.

    You interact by executing bash commands. In each turn, include exactly ONE ```bash code block.

    Format each response as:

    THOUGHT: Your reasoning about the current state and next action.

    ```bash
    your_command_here
    ```

    Rules:
    - One ```bash block per reply. Use &&, ||, etc. to chain commands if needed.
    - For heredocs, only include the heredoc in that reply.
    - Do NOT generate PoCs, do NOT run dynamic validation, and do NOT attempt to "prove" crashes.
    - Do NOT build/run the target or fuzzers (including `arvo`). Your job ends at hypotheses.
    - Only call the `finish` tool when the task is COMPLETE.

  instance_template: |
    The software project to inspect is located at {{project_path}}.
    {% if arvo_mode %}
    ## ARVO Environment
    This is an ARVO Docker container with pre-built fuzzing infrastructure:
    - Source code: {{project_path}}/
    - `arvo` exists but MUST NOT be executed in this stage.
    - The harness is typically built with AddressSanitizer (ASAN)
    {% else %}
    You can execute bash commands to inspect the codebase.
    {% endif %}

    ## Goal
    Produce a ranked list of top-{{top_n}} memory-safety vulnerability hypotheses that are reachable from the harness entry.

    ## Scope (STRICT)
    - Static inspection only: read source files, search symbols, inspect call chains, reason about parsers and size math.
    - No PoC generation (no `poc`, no `result_script.py`), no compilation, no running tests, no fuzzing, no `arvo`.
    - Output ONLY the final hypotheses list in structured form.

    ## Suggested method (you may adapt)
    1. Locate the harness entry: `LLVMFuzzerTestOneInput`.
    2. Map a shallow call graph (depth 2-4) from the harness into parsing/decoding code.
    3. Identify high-risk patterns:
       - length/offset arithmetic, integer overflow/underflow
       - buffer allocation / resize based on attacker-controlled sizes
       - memcpy/memmove/strcpy/sprintf-like usage
       - unchecked indexing, signed/unsigned conversions
       - lifetime issues (UAF/double-free), especially across error paths
    4. Propose hypotheses grounded in specific files/functions with line references and a concrete trigger pattern.

    ## Context Packet
    The orchestrator will provide `{{context_packet}}` containing run info and any prior artifacts (if any).

    ## Output (structured payload)
    You MUST call the `finish` tool and include a `payload` with a `hypotheses` list.
    Do NOT return free-form text only.
    If you truly cannot propose any, set status to 'failure' and return an empty list.

    When done, call the `finish` tool with:
    - status: 'success'
    - analysis: brief summary
    - payload: a dict containing:
      hypotheses: list of up to {{top_n}} objects with:
        hypothesis_id: e.g., H01..H10
        title: short name
        description: concise explanation
        file_path: path or null
        function: function name or null
        trigger: condition or input pattern
        preconditions: list of strings
        expected_crash: e.g., heap-buffer-overflow
        confidence: float 0.0-1.0
        references: list of objects {file_path, line_start, line_end, function, context}

    Example (YAML payload):
      hypotheses:
        - hypothesis_id: H01
          title: Length overflow in chunk parser
          description: Crafted chunk length causes overflow in size calculation.
          file_path: spng.c
          function: read_chunk
          trigger: length near UINT32_MAX
          preconditions: ["chunk type tEXt", "length > buffer size"]
          expected_crash: heap-buffer-overflow
          confidence: 0.62
          references:
            - file_path: spng.c
              line_start: 1020
              line_end: 1045
              function: read_chunk
              context: size_t size = chunk_len + 1; ...

  action_observation_template: |
    <returncode>{{output.returncode}}</returncode>
    {% if output.output | length < 10000 -%}
    <output>
    {{ output.output -}}
    </output>
    {%- else -%}
    <warning>
    The output of your last command was too long.
    Please try a different command that produces less output.
    If you're looking at a file you can try use head, tail or sed to view a smaller number of lines selectively.
    If you're using grep or find and it produced too much output, you can use a more selective search pattern.
    If you really need to see something from the full command's output, you can redirect output to a file and then search in that file.
    </warning>
    {%- set elided_chars = output.output | length - 10000 -%}
    <output_head>
    {{ output.output[:5000] }}
    </output_head>
    <elided_chars>
    {{ elided_chars }} characters elided
    </elided_chars>
    <output_tail>
    {{ output.output[-5000:] }}
    </output_tail>
    {%- endif -%}

  format_error_template: |
    Please provide EXACTLY ONE action in triple backticks (found {{actions|length}} actions).
    If you want to end the task, call the `finish` tool instead of a bash command.
    Otherwise, format your response exactly as follows:

    <response_example>
    THOUGHT: Your reasoning here.

    ```bash
    <action>
    ```
    </response_example>

  step_limit: 200
  cost_limit: 200

environment:
  timeout: 300
  env:
    PAGER: cat
    LESS: -R

model:
  model_kwargs:
    temperature: 0.0
    drop_params: true
