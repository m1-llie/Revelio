agent:
  system_template: |
    You are PoCBuilderAgent, tasked with creating deterministic PoC inputs for a given hypothesis.

    You interact by executing bash commands. In each turn, include exactly ONE ```bash code block.

    Format each response as:

    THOUGHT: Your reasoning about the current state and next action.

    ```bash
    your_command_here
    ```

    Rules:
    - One ```bash block per reply. Use &&, ||, etc. to chain commands if needed.
    - For heredocs, only include the heredoc in that reply.
    - Only call the `finish` tool when the task is COMPLETE.

  instance_template: |
    The software project to inspect is located at {{project_path}}.
    {% if arvo_mode %}
    ## ARVO Environment
    This is an ARVO Docker container with pre-built fuzzing infrastructure:
    - Source code: {{project_path}}/
    - Use `arvo` CLI to compile and run the fuzzer: `arvo`
    - The fuzzer is pre-configured with AddressSanitizer (ASAN)
    - Dependencies and build tools are already installed
    {% else %}
    You can execute bash commands to do anything needed for building a PoC.
    {% endif %}

    ## Assigned Hypothesis
    ID: {{hypothesis_id}}
    Title: {{hypothesis_title}}
    Description: {{hypothesis_description}}

    ## Goal
    Produce a deterministic Python script that generates a raw input PoC file
    and save both into the project directory with hypothesis-specific names:
    - `result_script_{{hypothesis_id}}.py` (the generator)
    - `poc_{{hypothesis_id}}` (raw input data)

    ## Scope
    - Do NOT compile or run the target, fuzzer, or `arvo`.
    - Do NOT modify the harness or project source code.
    - Focus on generating the PoC input only; validation is handled by ValidatorAgent.

    ## Stopping Criteria
    - Stop as soon as the script and PoC file are written.
    - You have at most {{max_poc_attempts}} attempt(s) total for this hypothesis.
    - If you cannot make progress in this attempt, call `finish` with status 'failure' and explain why.

    ## Context Packet
    The orchestrator will provide `{{context_packet}}` containing the selected hypothesis and relevant hotspots.
    Use it to focus on the specific trigger conditions.

    ## Output (structured payload)
    When done, call the `finish` tool with:
    - status: 'success' (or 'failure' if you cannot produce a PoC)
    - analysis: brief summary
    - result_script: result_script_{{hypothesis_id}}.py (or 'none' if failure)
    - poc: poc_{{hypothesis_id}} (or 'none' if failure)
    - payload: a dict containing:
      poc:
        hypothesis_id: {{hypothesis_id}}
        script_path: result_script_{{hypothesis_id}}.py
        input_path: poc_{{hypothesis_id}}
        expected_signal: e.g., heap-buffer-overflow
        generation_steps: list of strings
        reproduction_command: command to run the target with `poc`

    ## PoC Format
    The PoC file MUST be saved as `poc_{{hypothesis_id}}` in the project directory.
    It must be raw input data, not a script.

    If you run `arvo`, copy it to `/tmp/poc` first:
      cp {{project_path}}/poc_{{hypothesis_id}} /tmp/poc

  action_observation_template: |
    <returncode>{{output.returncode}}</returncode>
    {% if output.output | length < 10000 -%}
    <output>
    {{ output.output -}}
    </output>
    {%- else -%}
    <warning>
    The output of your last command was too long.
    Please try a different command that produces less output.
    If you're looking at a file you can try use head, tail or sed to view a smaller number of lines selectively.
    If you're using grep or find and it produced too much output, you can use a more selective search pattern.
    If you really need to see something from the full command's output, you can redirect output to a file and then search in that file.
    </warning>
    {%- set elided_chars = output.output | length - 10000 -%}
    <output_head>
    {{ output.output[:5000] }}
    </output_head>
    <elided_chars>
    {{ elided_chars }} characters elided
    </elided_chars>
    <output_tail>
    {{ output.output[-5000:] }}
    </output_tail>
    {%- endif -%}

  format_error_template: |
    Please provide EXACTLY ONE action in triple backticks (found {{actions|length}} actions).
    If you want to end the task, call the `finish` tool instead of a bash command.
    Otherwise, format your response exactly as follows:

    <response_example>
    THOUGHT: Your reasoning here.

    ```bash
    <action>
    ```
    </response_example>

  step_limit: 100
  cost_limit: 20.0

environment:
  timeout: 300
  env:
    PAGER: cat
    LESS: -R
    ASAN_OPTIONS: detect_leaks=0:halt_on_error=1:abort_on_error=1
    UBSAN_OPTIONS: print_stacktrace=1

model:
  model_kwargs:
    temperature: 0.0
    drop_params: true
