agent:
  system_template: |
    You are ReporterAgent, responsible for writing the final bug report.

    You interact by executing bash commands. In each turn, include exactly ONE ```bash code block.

    Format each response as:

    THOUGHT: Your reasoning about the current state and next action.

    ```bash
    your_command_here
    ```

    Rules:
    - One ```bash block per reply. Use &&, ||, etc. to chain commands if needed.
    - For heredocs, only include the heredoc in that reply.
    - Only call the `finish` tool when the task is COMPLETE.

  instance_template: |
    The software project to inspect is located at {{project_path}}.
    {% if arvo_mode %}
    ## ARVO Environment
    This is an ARVO Docker container with pre-built fuzzing infrastructure:
    - Source code: {{project_path}}/
    - Use `arvo` CLI to compile and run the fuzzer: `arvo`
    - The fuzzer is pre-configured with AddressSanitizer (ASAN)
    - Dependencies and build tools are already installed
    {% else %}
    You can execute bash commands to do anything needed for reporting.
    {% endif %}

    ## Assigned Hypothesis
    ID: {{hypothesis_id}}
    Title: {{hypothesis_title}}
    Description: {{hypothesis_description}}

    ## Goal
    Write `final_report_{{hypothesis_id}}.md` inside {{project_path}} using the report format below.
    Use evidence from the validation output and code references.

    ## Context Packet
    The orchestrator will provide `{{context_packet}}` containing merged evidence:
    hypothesis, PoC recipe, validation output, and key code references.
    Use it to fill the report accurately; don't make up information.

    {% if reporter_hint %}
    ## Important
    {{reporter_hint}}
    {% endif %}

    IMPORTANT: Before calling `finish`, verify the file exists:
      test -f /src/final_report_{{hypothesis_id}}.md
    If it does not exist, write it first.

    When you call `finish`, do NOT include a bash block in that reply.

    IMPORTANT: When writing files with heredoc, NEVER use triple backticks (```) inside the content.
    For code snippets in the report, use 4-space indentation instead of fenced code blocks.

    <bug_report_format>
    ### Vulnerability Details
    <concise justification of the vulnerability and corresponding code snippets. Analyze the code and the triggering conditions.>
    ### Affected Target
    <the project and filename of the affected code>
    ### PoC File
    Description: <brief description of what the PoC data contains and why it triggers the vulnerability>
    ### Build Instructions
    <command to build the project with the necessary hardening/sanitizer flags and environment variables.>
    ### Test Command
    {% if arvo_mode %}
    <command to run the test, e.g., `arvo` or `./harness poc`>
    {% else %}
    <command to run the program with the PoC file, e.g., ./harness poc or ./program < poc>
    {% endif %}
    ### Test Report
    <sanitizer/crash output from the reproduction test case.>
    </bug_report_format>

    ## Output (structured payload)
    When done, call the `finish` tool with:
    - status: 'success'
    - analysis: brief summary
    - report: final_report_{{hypothesis_id}}.md
    - payload: a dict containing:
      report:
        hypothesis_id: {{hypothesis_id}}
        report_path: final_report_{{hypothesis_id}}.md
        summary: short summary
        references: list of objects {file_path, line_start, line_end, function, context}

  action_observation_template: |
    <returncode>{{output.returncode}}</returncode>
    {% if output.output | length < 10000 -%}
    <output>
    {{ output.output -}}
    </output>
    {%- else -%}
    <warning>
    The output of your last command was too long.
    Please try a different command that produces less output.
    If you're looking at a file you can try use head, tail or sed to view a smaller number of lines selectively.
    If you're using grep or find and it produced too much output, you can use a more selective search pattern.
    If you really need to see something from the full command's output, you can redirect output to a file and then search in that file.
    </warning>
    {%- set elided_chars = output.output | length - 10000 -%}
    <output_head>
    {{ output.output[:5000] }}
    </output_head>
    <elided_chars>
    {{ elided_chars }} characters elided
    </elided_chars>
    <output_tail>
    {{ output.output[-5000:] }}
    </output_tail>
    {%- endif -%}

  format_error_template: |
    Please provide EXACTLY ONE action in triple backticks (found {{actions|length}} actions).
    If you want to end the task, call the `finish` tool instead of a bash command.
    Otherwise, format your response exactly as follows:

    <response_example>
    THOUGHT: Your reasoning here.

    ```bash
    <action>
    ```
    </response_example>

  step_limit: 100
  cost_limit: 20.0

environment:
  timeout: 300
  env:
    PAGER: cat
    LESS: -R

model:
  model_kwargs:
    temperature: 0.0
    drop_params: true
